packages:
  "framer-motion": "For smooth animations of tree nodes, answer reveals, and page transitions"
  "clsx": "For conditional class name merging"
  "tailwind-merge": "For merging tailwind utility classes safely"
  "lucide-react": "For beautiful consistent iconography"

images:
  - path: "artifacts/exam-roadmap/public/images/academic-bg.png"
    description: "A very subtle, abstract, minimal geometric background in clean white and very pale ice blue, suggesting an academic or professional learning environment. High key lighting, soft gradients, no harsh lines. Premium and modern."
    aspect_ratio: "16:9"

notes:
  - Assumes @workspace/api-client-react provides all TanStack Query hooks
  - Relies on localStorage for simple auth persistence
  - Relies on sessionStorage to prevent duplicate event tracking per session
