You are a senior full-stack developer. Build a production-ready MVP using Next.js (App Router) and TypeScript.

OBJECTIVE:
Build an exam-oriented roadmap platform where students navigate a hierarchical syllabus and view subtopics with explanation + 2-mark and 5-mark questions and answers. Track when a student consumes a subtopic and store that event in Supabase.

STRICT CONSTRAINTS:

* Use Next.js (latest, App Router)
* Use TypeScript
* Use Supabase (external project)
* DO NOT use any Replit-specific APIs, DB, or auth
* Must be fully portable to Vercel without changes
* Use .env.local for secrets

ENV VARIABLES:
NEXT_PUBLIC_SUPABASE_URL=your_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_key

PROJECT STRUCTURE:
Create:

/app
/page.tsx
/roadmap/page.tsx
/subtopic/[id]/page.tsx

/components
RoadmapTree.tsx
NodeItem.tsx
SubtopicView.tsx
QuestionBlock.tsx
AnswerToggle.tsx

/lib
supabaseClient.ts
eventTracker.ts

/data
universities.ts

/types
index.ts

DATA MODELS:

University:
type University = {
id: string;
name: string;
};

Config:
type Config = {
id: string;
universityId: string;
year: string;
branch: string;
exam: "mid1" | "mid2" | "endsem";
isActive: boolean;
};

Node:
type Node = {
id: string;
title: string;
type: "unit" | "topic" | "subtopic";
parentId?: string;
};

SubtopicContent:
type SubtopicContent = {
id: string;
explanation: string;
twoMark: {
question: string;
answer: string;
};
fiveMark: {
question: string;
answer: string;
};
};

UNIVERSITIES (STATIC LIST):
Create a constant list of universities with id + name. Do not allow free text input.

ROADMAP RULES:

* Structure: Subject → Exam → Units → Topics → Subtopics
* Only show configs where isActive = true
* Disabled configs should show “Not Available”

UI BEHAVIOR:

Landing Page:

* Select university, year, branch, exam

Roadmap Page:

* Tree navigation (expandable)
* Clicking a subtopic opens subtopic page

Subtopic Page:
Display:

1. Explanation
2. 2-mark question
3. 5-mark question

Answers:

* Hidden by default
* Toggle button to reveal

EVENT TRACKING (CRITICAL):

Track when a student consumes a subtopic.

Event Name:
SUBTOPIC_CONSUMED

Trigger:

* User scrolls to bottom of subtopic
* AND stays for at least 2 seconds

Use IntersectionObserver.

Fire event only once per subtopic per session.

EVENT PAYLOAD:
{
user_id,
university_id,
year,
branch,
exam,
subject_id,
topic_id,
subtopic_id,
timestamp
}

SUPABASE:

Create client in /lib/supabaseClient.ts using official SDK.

Create function in /lib/eventTracker.ts:

trackSubtopicConsumed(payload)

Insert event into Supabase table: events

Do NOT block UI while sending event.

AUTH (MVP):

Login screen:

* Username = college ID
* Password = "1234567890"

Store logged-in user in localStorage.

Supabase users table:
id (college_id), university_id, branch, year

ADMIN VIEW (SIMPLE):

Create a basic admin page:

* List subtopics
* Show count of events per subtopic

NO advanced analytics needed.

IMPORTANT RULES:

* Keep UI minimal and fast
* Do not over-engineer
* Do not add unnecessary libraries
* Do not tightly couple logic with Supabase
* Keep everything modular

MVP REQUIREMENTS:

* Config selection
* Roadmap tree
* Subtopic page
* 2M + 5M Q&A display
* Answer toggle
* Scroll-based event tracking
* Supabase integration

FINAL GOAL:
A clean, working app where:

* Students navigate roadmap
* View subtopics
* Read questions + answers
* System tracks subtopic consumption in Supabase

Ensure code is clean, modular, and deployable to Vercel without changes.
